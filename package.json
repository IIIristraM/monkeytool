{
  "name": "monkeytool",
  "packageManager": "yarn@3.5.1",
  "workspaces": [
    "./services/*"
  ],
  "scripts": {
    "init": "yarn",
    "start": "docker compose build && docker compose up"
  }
}
